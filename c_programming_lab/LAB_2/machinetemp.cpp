#include <stdio.h>
int main() {
    float a;
    printf(" enter the temparture of the machinen in Celcius :");
    scanf("%f",&a);
    if (a > 60 && a <= 80){
    printf{" the machine is running safe range"};
    }
    else if printf("WARNING");
}
    
    

    
